# 📄 เอกสารข้อกำหนดความต้องการ (Requirements Specification)

**ชื่อโปรเจกต์:** Auto Watermark & Resize Tool สำหรับทีม Content (Windows Standalone App)
**เทคโนโลยีที่กำหนด (Tech Stack):** Python 3.x, CustomTkinter (สำหรับทำ UI), Pillow (สำหรับจัดการรูปภาพ), PyInstaller (สำหรับแพ็กเป็น .exe)
**กำหนดส่งงาน:** ภายในวันเสาร์นี้

## 1. คุณสมบัติหลักของโปรแกรม (Functional Requirements)

โปรแกรมจะต้องสามารถทำงานอัตโนมัติได้ตามเงื่อนไขดังต่อไปนี้:

* **1.1 การนำเข้าภาพ (Import):** รองรับการเลือกทั้งโฟลเดอร์ (Select Folder) และสามารถอ่านไฟล์รูปภาพนามสกุลยอดฮิตได้อย่างน้อย `.jpg`, `.jpeg` และ `.png`
* **1.2 การปรับขนาด (Resize):** บังคับลดหรือขยายขนาดภาพ ให้มี **ความกว้าง (Width) ที่ 1280 px เสมอ** ส่วนความสูง (Height) ให้คำนวณอัตโนมัติตามสัดส่วนเดิม (Maintain Aspect Ratio) เพื่อไม่ให้ภาพบิดเบี้ยว
* **1.3 การแยกแยะรูปทรง (Orientation Logic):** โปรแกรมต้องตรวจสอบความกว้างและความสูงของภาพต้นฉบับได้:
* ถ้า ความกว้าง > ความสูง = ถือเป็น **ภาพแนวนอน (Landscape)**
* ถ้า ความสูง >= ความกว้าง = ถือเป็น **ภาพแนวตั้ง หรือ จัตุรัส (Portrait / Square)**


* **1.4 การใส่โลโก้ (Watermark Placement):** ต้องแยกตำแหน่งการวางโลโก้ตามรูปทรงภาพอย่างชัดเจน (Dev สามารถฮาร์ดโค้ดตั้งค่าตำแหน่งเริ่มต้นมาให้ หรือทำช่องให้กรอก X,Y ในโปรแกรมก็ได้):
* **สำหรับภาพแนวนอน:** วางโลโก้ที่ตำแหน่ง A (เช่น มุมขวาล่าง ห่างจากขอบ 50px)
* **สำหรับภาพแนวตั้ง:** วางโลโก้ที่ตำแหน่ง B (เช่น ตรงกลางด้านบน ห่างจากขอบ 50px)
* *หมายเหตุ: โลโก้ที่นำมาแปะ จะเป็นไฟล์ .png พื้นหลังใส โปรแกรมต้องรองรับการอ่านค่าความโปร่งใส (Alpha channel) เพื่อไม่ให้ติดพื้นหลังสีดำหรือขาว*


* **1.5 การส่งออก (Export):** เมื่อทำเสร็จ ต้องไม่บันทึกทับไฟล์เดิมเด็ดขาด แต่ให้สร้างโฟลเดอร์ย่อยข้างในโฟลเดอร์ต้นฉบับ ชื่อว่า `[ชื่อโฟลเดอร์เดิม]_Processed` แล้วบันทึกรูปทั้งหมดลงในนั้น ด้วยคุณภาพ (Quality) 90-100%

---

## 2. การออกแบบหน้าจอ (UI/UX Requirements)

เนื่องจากผู้ใช้งานคือทีม Content ที่เน้นความรวดเร็ว หน้าจอโปรแกรมต้อง **"มินิมอล มีปุ่มให้น้อยที่สุด และไม่ต้องพิมพ์อะไรเลย"** โดยหน้าจอควรมีองค์ประกอบดังนี้:

1. **ส่วนที่ 1: นำเข้าโลโก้** - มีปุ่ม `[เลือกไฟล์โลโก้ .png]` พร้อมแสดงชื่อไฟล์ที่เลือก
2. **ส่วนที่ 2: นำเข้าภาพต้นฉบับ** - มีปุ่ม `[เลือกโฟลเดอร์รูปภาพ]` พร้อมแสดงจำนวนภาพที่โปรแกรมค้นพบในโฟลเดอร์นั้น
3. **ส่วนที่ 3: ตำแหน่งโลโก้ (Optional)** - มี Dropdown หรือให้กรอกตัวเลขง่ายๆ เผื่อทีม Content อยากขยับโลโก้ (ถ้างานด่วนมาก ให้ Dev ล็อกตำแหน่งมาเลยก็ได้)
4. **ส่วนที่ 4: สถานะ** - แถบโหลด (Progress Bar) หรือข้อความบอกว่ากำลังทำรูปที่เท่าไหร่ (เช่น `Processing... 5/100`)
5. **ส่วนที่ 5: ปุ่มเริ่มทำงาน** - ปุ่ม `[START / เริ่มประมวลผล]` ขนาดใหญ่ กดง่ายเห็นชัดเจน

---

## 3. 🛠️ วิธีการทำงานของโปรแกรมแบบจับมือทำ (Step-by-Step User Flow)

เพื่อให้ Dev เห็นภาพว่าทีม Content จะใช้งานอย่างไร นี่คือลำดับการทำงานตั้งแต่เปิดโปรแกรมจนจบงานครับ:

**ขั้นตอนที่ 1: เปิดโปรแกรมเตรียมพร้อม**

* ทีม Content ดับเบิลคลิกไฟล์ `AutoWatermark.exe` (ไม่ต้องติดตั้งโปรแกรมอื่นเพิ่มเติม หน้าต่างแอปเด้งขึ้นมาเลย)

**ขั้นตอนที่ 2: ใส่โลโก้**

* คลิกที่ปุ่ม **"เลือกไฟล์โลโก้"** บนหน้าจอ
* หน้าต่างเลือกไฟล์ของ Windows จะเด้งขึ้นมา (Windows Explorer) ให้ผู้ใช้คลิกเลือกไฟล์โลโก้บริษัท (ไฟล์ .png พื้นใส) แล้วกด Open
* *แอปจะจำไฟล์นี้ไว้เป็นตรายางเตรียมประทับ*

**ขั้นตอนที่ 3: เลือกโฟลเดอร์งาน**

* คลิกที่ปุ่ม **"เลือกโฟลเดอร์รูปภาพ"**
* หน้าต่างจะเด้งให้เลือกโฟลเดอร์ที่เก็บรูปถ่ายงานทั้งหมดเอาไว้ (สมมติว่าในนั้นมี 50 รูป มีทั้งแนวตั้งและแนวนอนปนกัน) แล้วกด Select Folder
* *แอปจะสแกนโฟลเดอร์นั้น แล้วแสดงข้อความว่า "พบรูปภาพทั้งหมด 50 ไฟล์"*

**ขั้นตอนที่ 4: สั่งลุยงาน!**

* ทีม Content กดปุ่ม **"START เริ่มประมวลผล"**
* *แอปจะเริ่มจับมือทำงานตามกระบวนการหลังบ้านดังนี้ (ทำงานวนไปทีละรูปจนครบ 50 รูป):*
1. หยิบรูปที่ 1 ขึ้นมา
2. ย่อขนาด (Resize) โดยบีบความกว้างให้เหลือ 1280 px พอดีเป๊ะ ส่วนความสูงให้โปรแกรมคำนวณย่อตามลงมาอัตโนมัติ
3. โปรแกรมจะเช็กว่า ตอนนี้รูปนี้ความกว้างมากกว่าความสูงไหม?
* ถ้ากว้างกว่า (แนวนอน) -> หยิบตรายาง (โลโก้) ไปประทับที่มุมขวาล่าง
* ถ้าสูงกว่า (แนวตั้ง) -> หยิบตรายาง (โลโก้) ไปประทับที่มุมขวาบน


4. สร้างโฟลเดอร์ใหม่เตรียมรอไว้ (ถ้ายังไม่มี)
5. เซฟรูปที่ประทับตราแล้วลงโฟลเดอร์ใหม่
6. หน้าจอจะอัปเดตแถบความคืบหน้า (Progress Bar) ขยับไปที่รูปที่ 2, 3, 4... ไปเรื่อยๆ



**ขั้นตอนที่ 5: รับผลงาน**

* เมื่อทำครบ 50 รูป โปรแกรมจะขึ้นหน้าต่างแจ้งเตือน (Popup) ว่า **"เสร็จสิ้น! ประมวลผลรูปภาพเรียบร้อยแล้ว"** พร้อมมีปุ่มให้กด **"เปิดโฟลเดอร์ผลลัพธ์"** (Open Output Folder)
* เมื่อคลิกปุ่มนั้น หน้าต่างโฟลเดอร์ Windows จะเด้งขึ้นมาพร้อมให้ทีม Content เอารูปทั้ง 50 รูปที่ย่อและแปะโลโก้แล้ว ไปโพสต์ลงเพจได้ทันที!

---

## 4. เงื่อนไขการส่งมอบงานสำหรับ Dev (Deliverables)

1. **ไฟล์สำหรับใช้งานจริง:** ไฟล์ `.exe` ที่สามารถนำไปรันบน Windows 10/11 ได้ทันที โดยไม่ต้องให้ฝั่งผู้ใช้ (ทีม Content) ติดตั้ง Python, ตัวแปรสภาพแวดล้อม (Environment Variables) หรือไลบรารีใดๆ ให้ยุ่งยาก
2. **ซอร์สโค้ด (Source Code):** ส่งมอบไฟล์โค้ด `.py` ทั้งหมด รวมถึงไฟล์ `requirements.txt` เผื่อมีการอัปเดตแก้ไขในอนาคต
3. **ความเร็วและความเสถียร:** โปรแกรมต้องไม่ค้าง (Not Responding) ระหว่างที่กำลังประมวลผลรูปภาพจำนวนมากๆ (Dev ควรใช้การทำ Multi-threading หรือจัดการ Process ให้อยู่เบื้องหลัง เพื่อให้ UI ยังกดและแสดงแถบโหลดได้)

---

นำข้อความทั้งหมดนี้ก๊อปปี้ไปโพสต์ประกาศหาคนเขียน หรือส่งให้ Dev ในทีมดูได้เลยครับ สเปกชัดเจนระดับนี้ Dev ประเมินเวลาและลงมือเขียนโค้ดได้จบภายใน 1-2 วันแน่นอนครับ!